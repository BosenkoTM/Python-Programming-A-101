
# Задание task_03_04_02.
#
# Выполнил: Фамилия И.О.
# Группа: !!!

import sys

def load_data(filename):
    """Загрузить список вещественных чисел из файла 'filename'.

    Функция не обрабатывает исключения."""
    # Удалите комментарий и допишите код


def append_to_file(values, filename):
    """Дописать данные в файл.

    Параметры:
        - values (list of float): список вещественных чисел;
        - filename (str): имя файла.
    """
    # 'a' открывает файл в режиме добавления (append).
    # Здесь filename будет 'output.txt', который будет создан или перезаписан в текущей директории.
    # Удалите комментарий и допишите код


try:
    # Coderunner подает имя ВХОДНОГО файла как первую строку ввода
    input_filename = input()
    # Фиксированное имя для ВЫХОДНОГО файла, который мы будем записывать
    output_filename = "output.txt"
    
    # Загружаем данные из входного файла
    values = load_data(input_filename)

    # Дописываем загруженные данные в ВЫХОДНОЙ файл
    append_to_file(values, output_filename)
    
    # Сообщение об успешном выполнении, которое Coderunner будет проверять
    print(f"Данные успешно обработаны из '{input_filename}' и добавлены в файл '{output_filename}'.")

except FileNotFoundError:
    print("Указанный входной файл не существует.") # Уточняем сообщение

except (IOError, ValueError) as err:
    # Здесь перехватываются как ошибки ввода-вывода (IOError), так и
    # ошибки преобразования чисел (ValueError) при чтении из файла.
    print("Ошибка при чтении входного файла или записи выходного файла:", err) # Уточняем сообщение

except EOFError:
    # Если Coderunner подает пустой ввод (нет имени файла)
    print("Ошибка ввода: Имя входного файла не указано.") # Уточняем сообщение

except Exception as err:
    print("Произошла непредвиденная ошибка!")
    print("Тип:", type(err))
    print("Описание:", err)